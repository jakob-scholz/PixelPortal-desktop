#include "Player.h"

#include <Arduboy2.h>
#include "Globals.h"

void Player::move()
{
  if (arduboy.pressed(UP_BUTTON))
  {
  }

  if (arduboy.pressed(DOWN_BUTTON))
  {
  }

  if (arduboy.justPressed(A_BUTTON))
  {
  }
}
